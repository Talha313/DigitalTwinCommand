/**
 * Canned example prompts shown on the chat empty state. This is a static UI
 * convenience only — not fake backend data — the conversation, messages, and
 * streaming are wired to the real backend in `@/lib/chat`.
 */
export const suggestedPrompts: string[] = [
  "Summarize this morning's market moves",
  "Draft a follow-up to yesterday's investor call",
  "What workflows are waiting on my approval?",
  "Prep three talking points for the earnings call",
];
